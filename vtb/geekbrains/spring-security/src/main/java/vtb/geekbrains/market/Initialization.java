package vtb.geekbrains.market;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.stream.Collectors;

public class Initialization {
    public Initialization() throws Exception {
//        MarketApp.factory.getCurrentSession().beginTransaction();
//        String request = Files.lines(Paths.get("market.sql")).collect(Collectors.joining(" "));
//        MarketApp.factory.getCurrentSession().createNativeQuery(request).executeUpdate();
//        MarketApp.factory.getCurrentSession().getTransaction().commit();
    }
}
