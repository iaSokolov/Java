# Read file on Config-Server:
# app-about-company.properties or app-about-company.yml
spring.application.name=product

# This is the default:
spring.cloud.config.uri=http://${CONFIG_SERVICE_HOST}:8888
management.security.enabled=false